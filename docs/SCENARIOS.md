# Scenarios

Scenarios live in `scenarios/` as JSON files.

Current fields are minimal and parsed with a lightweight extractor:
- `seed` (int)
- `ticks` (int)
- `world.width`, `world.height` (int)
- `units` list (positions)

Example: `tutorial_01.json`.
